//! Double all numbers in an array using map().
const numbers = [4, 8, 9, 5];
const DoubleNumber = numbers.map((element) => {
  return element * 2;
});
console.log(DoubleNumber, "DoubleNumber");

//!Convert an array of strings to uppercase usisng map().
const arrayStrings  = ["cat", "dogs", "lion","elephant"] 
const DoubleNumber = numbers.map((element) => {
  return element * 2;
});
// Add a new property to every object in an array using map().
// Extract only names from an array of user objects using map().
// Convert an array of prices into prices with GST added.
// Convert an array of numbers into their squares.
// Convert an array of dates into formatted date strings.
// Convert an array of strings into objects.
// Use map() to generate HTML elements from data.
// Convert an array of user objects into an array of IDs.
